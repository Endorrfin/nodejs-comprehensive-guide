/* Interview bank — seeded subset (grows to 40 in S8). Tagged by chapter + level. */
export interface BankItem {
  id: string;
  chapter: string; // chapter id it maps to
  topic: string; // display label
  level: "senior" | "staff";
  q: string;
  a: string;
}

export const INTERVIEW: BankItem[] = [
  {
    id: "el-phases",
    chapter: "event-loop",
    topic: "Event Loop",
    level: "senior",
    q: "Walk me through the phases of the event loop.",
    a: "Six phases per tick in fixed order: timers → pending callbacks → idle/prepare (internal) → poll → check → close. After every callback and between phases, microtasks drain: the nextTick queue first, then the Promise queue.",
  },
  {
    id: "el-order",
    chapter: "event-loop",
    topic: "Event Loop",
    level: "senior",
    q: "nextTick vs Promise.then vs setImmediate vs setTimeout(0) — what order?",
    a: "Synchronous code first. Then microtasks: all nextTick, then all promises. Then macrotasks by phase: setTimeout(0) in timers, setImmediate in check. In the main module timeout-vs-immediate is non-deterministic; inside an I/O callback setImmediate runs first.",
  },
  {
    id: "el-starve",
    chapter: "event-loop",
    topic: "Event Loop",
    level: "staff",
    q: "How can you starve the event loop?",
    a: "Recursively scheduling process.nextTick (or promises) keeps a microtask queue non-empty, so the loop never advances — timers and I/O never run. Long synchronous CPU work also blocks the single thread.",
  },
  {
    id: "el-v8",
    chapter: "event-loop",
    topic: "Event Loop",
    level: "senior",
    q: "Is the event loop part of V8?",
    a: "No. V8 executes JavaScript and manages its heap. The event loop, timers, the thread pool and OS I/O notifications are all libuv. Node wires them together.",
  },
  {
    id: "async-await-parallel",
    chapter: "async-model",
    topic: "Async model",
    level: "senior",
    q: "Does async/await make code run in parallel?",
    a: "No. await pauses the async function until a promise settles; it sequences awaited work. For concurrency, start the promises first and await them together (Promise.all).",
  },
  {
    id: "async-microtask",
    chapter: "async-model",
    topic: "Async model",
    level: "staff",
    q: "Where do awaited continuations run relative to timers?",
    a: "A resolved await continuation is a microtask, so it runs before any timer/macrotask — at the next microtask checkpoint, after the current synchronous run.",
  },
  {
    id: "gc-generations",
    chapter: "v8-gc",
    topic: "V8 & GC",
    level: "senior",
    q: "Explain V8's generational garbage collection.",
    a: "Objects are allocated in young space and collected by a fast scavenger (Scavenge); survivors are promoted to old space, collected by mark-sweep-compact. The generational hypothesis: most objects die young.",
  },
  {
    id: "gc-shapes",
    chapter: "v8-gc",
    topic: "V8 & GC",
    level: "staff",
    q: "Why keep object shapes stable for performance?",
    a: "V8 uses hidden classes and inline caches keyed on an object's shape. Adding/removing properties in varying order creates new hidden classes, deopting inline caches and slowing property access.",
  },
  {
    id: "conc-threads-vs-proc",
    chapter: "concurrency",
    topic: "Concurrency",
    level: "senior",
    q: "worker_threads vs cluster vs child_process — when each?",
    a: "worker_threads for CPU-bound work sharing memory in-process; cluster to fork processes sharing a server port across cores; child_process to run separate programs and talk over IPC/streams.",
  },
  {
    id: "conc-pool",
    chapter: "concurrency",
    topic: "Concurrency",
    level: "staff",
    q: "What uses the libuv thread pool, and how big is it?",
    a: "fs operations, dns.lookup, crypto and zlib use the pool (default 4 threads, configurable via UV_THREADPOOL_SIZE). Most network I/O does NOT — the kernel handles it asynchronously without pool threads.",
  },
  {
    id: "streams-backpressure",
    chapter: "streams",
    topic: "Streams",
    level: "senior",
    q: "What is backpressure and how do you respect it?",
    a: "When a writable's buffer passes highWaterMark, write() returns false; you should stop writing and wait for the 'drain' event. pipeline()/pipe() handle this for you and (pipeline) also clean up on error.",
  },
  {
    id: "modules-cjs-esm",
    chapter: "modules",
    topic: "Modules",
    level: "senior",
    q: "Key differences between CommonJS and ESM?",
    a: "CJS require() is synchronous, cached, returns a value copy of module.exports, and has __dirname. ESM import is asynchronous, statically analyzable, exposes live read-only bindings, and uses import.meta.url; top-level await is ESM-only.",
  },
  {
    id: "errors-types",
    chapter: "errors",
    topic: "Errors",
    level: "staff",
    q: "How do you decide whether to crash on an error?",
    a: "Classify it: operational errors (bad input, network failures) are expected — handle and continue. Programmer errors (bugs, invariant violations) should fail fast: log and let the process crash so a supervisor restarts a clean state.",
  },
  {
    id: "http-timeouts",
    chapter: "http",
    topic: "HTTP",
    level: "staff",
    q: "Why might a healthy Node service return sporadic 502s behind a proxy?",
    a: "Often a timeout mismatch: the upstream proxy keep-alive outlives Node's keepAliveTimeout/headersTimeout, so Node closes a socket the proxy reuses. Align the timeout triad (keepAliveTimeout ≥ proxy idle, headersTimeout > keepAliveTimeout).",
  },
  {
    id: "gc-tiers-deopt",
    chapter: "v8-gc",
    topic: "V8 & GC",
    level: "staff",
    q: "Name V8's compilation tiers and explain deoptimization.",
    a: "Ignition (bytecode interpreter) → Sparkplug (baseline JIT) → Maglev (mid-tier optimizing JIT, default in Node 22) → TurboFan (peak optimizer). Optimizing tiers speculate on observed types/shapes; when a runtime value breaks an assumption, V8 deoptimizes — discards the optimized code and falls back to a lower tier. Repeated deopt churn is slower than never optimizing, so keep types and object shapes stable.",
  },
  {
    id: "gc-leak-hunt",
    chapter: "v8-gc",
    topic: "V8 & GC",
    level: "senior",
    q: "Memory climbs until the process OOMs — how do you find the leak?",
    a: "Confirm it's a leak (rising retained floor across heap snapshots, not just sawtoothing heap-used), then diff snapshots over time and sort by retained size to find the retainer — usually a module-level cache/Map that only grows, per-request listeners never removed, or closures held by timers. Fix the retainer; raising --max-old-space-size only delays the crash and lengthens major-GC pauses.",
  },
  {
    id: "conc-dns-pool",
    chapter: "concurrency",
    topic: "Concurrency",
    level: "staff",
    q: "How can DNS stall a busy HTTP client, and how do you fix it?",
    a: "http/net resolve hostnames via dns.lookup, which wraps the blocking getaddrinfo and runs on the 4-thread libuv pool. Many lookups to new hosts can exhaust the pool and block unrelated fs/crypto work. Use dns.resolve*() (c-ares, network-based, no pool thread) or a caching resolver, cache results, and raise UV_THREADPOOL_SIZE.",
  },
  {
    id: "conc-worker-comm",
    chapter: "concurrency",
    topic: "Concurrency",
    level: "staff",
    q: "How do worker_threads share data, and what does it cost?",
    a: "By default postMessage makes a structured-clone copy, so large payloads cost serialization and memory. You can transfer an ArrayBuffer (zero-copy, ownership moves) or use a SharedArrayBuffer with Atomics for true shared memory and lock-free coordination. Workers never share ordinary variables — design around copies or shared buffers.",
  },
  {
    id: "streams-pipe-vs-pipeline",
    chapter: "streams",
    topic: "Streams",
    level: "staff",
    q: "Why prefer pipeline() over pipe(), and what does write()'s return value mean?",
    a: "write() returns false once the writable's buffered bytes reach highWaterMark — a signal to stop and wait for 'drain'. .pipe() wires data through and forwards backpressure but does NOT destroy the chain on error, so a failure mid-stream leaks file handles/sockets. stream.pipeline() (or its promises form) forwards backpressure AND destroys every stream on error or completion, giving you a single error path and no leaks.",
  },
  {
    id: "streams-types",
    chapter: "streams",
    topic: "Streams",
    level: "senior",
    q: "What are the stream types, and what is objectMode?",
    a: "Readable (source), Writable (sink), Duplex (both, independent sides — a TCP socket), and Transform (a Duplex whose output is a function of its input — gzip, hashing). By default streams move Buffers/strings with a 64 KiB highWaterMark; in objectMode each chunk is an arbitrary JS value and highWaterMark counts objects (default 16).",
  },
  {
    id: "modules-require-esm",
    chapter: "modules",
    topic: "Modules",
    level: "staff",
    q: "Can you require() an ES module now, and what's the dual-package hazard?",
    a: "Yes — require(esm) is unflagged and stabilized in current LTS lines (it landed in 22.12); it throws ERR_REQUIRE_ASYNC_MODULE only if the target uses top-level await. The dual-package hazard is shipping both a CJS and an ESM build: a dependency loaded both ways yields two separate module instances with separate state (two caches, failing instanceof). Mitigate by exposing state from a single CJS core, or shipping ESM-only.",
  },
  {
    id: "modules-live-binding",
    chapter: "modules",
    topic: "Modules",
    level: "staff",
    q: "Live bindings vs value copies — how do CJS and ESM differ on mutation and circular deps?",
    a: "ESM imports are live read-only bindings to the exporter's variables, so they observe later mutations; CommonJS require() returns a snapshot value-copy of module.exports. On circular deps, CJS hands back a partial exports object (you may read undefined for not-yet-assigned members), while ESM links bindings before evaluation so hoisted declarations resolve — circular references are handled more gracefully.",
  },
  {
    id: "wn-define",
    chapter: "what-is-node",
    topic: "Foundations",
    level: "senior",
    q: "What is Node.js, and is it single- or multi-threaded?",
    a: "A JavaScript runtime built on V8 that runs JS outside the browser with non-blocking I/O via libuv. Your application JavaScript runs on a single event-loop thread; libuv adds a small thread pool (default 4) for blocking ops and uses the kernel for sockets. So: single-threaded JS, with offloaded and optional (worker_threads) parallelism beneath.",
  },
  {
    id: "wn-vs-framework",
    chapter: "what-is-node",
    topic: "Foundations",
    level: "senior",
    q: "What's the difference between Node and Express?",
    a: "Node is the runtime — it provides V8, the event loop, and core modules (http, fs, streams). Express/Nest/Fastify are frameworks: libraries running on Node that add routing, middleware and structure. You can build a full server with Node's http module alone; frameworks just make it ergonomic.",
  },
  {
    id: "str-c10k",
    chapter: "strengths",
    topic: "Foundations",
    level: "staff",
    q: "If Node is single-threaded, how does it serve thousands of connections?",
    a: "Network sockets are non-blocking: libuv arms them with the OS notifier (epoll/kqueue/IOCP) and the kernel signals readiness; one loop thread multiplexes them all, holding no thread per connection. Memory per connection is a few KB of socket state vs ~1 MB per thread in a thread-per-request model, so the same box scales to tens of thousands — the C10k win.",
  },
  {
    id: "weak-block",
    chapter: "weaknesses",
    topic: "Foundations",
    level: "staff",
    q: "What happens to other requests during a 250 ms synchronous CPU task?",
    a: "They all wait. The event loop can't advance while the one thread is busy, so every in-flight callback and queued timer is delayed by up to the full 250 ms and no new connections are accepted. One heavy synchronous call therefore adds latency across the board — visible as p99 spikes and event-loop lag. Offload it to a worker_thread or chunk it.",
  },
  {
    id: "weak-cpu",
    chapter: "weaknesses",
    topic: "Foundations",
    level: "senior",
    q: "What is Node's biggest weakness and how do you work around it?",
    a: "CPU-bound work: all JS runs on one thread, so a long synchronous computation blocks the whole process. Work around it by keeping handlers async and short, and moving heavy compute off the loop — a pool of worker_threads, chunking across ticks, a native addon, or a separate service.",
  },
  {
    id: "comp-deno-bun",
    chapter: "competitors",
    topic: "Foundations",
    level: "senior",
    q: "Node vs Deno vs Bun — what actually differs?",
    a: "All three are event-loop JS/TS runtimes. Node (V8) has the deepest ecosystem and track record. Deno (V8) runs TypeScript directly, is secure-by-default via opt-in permissions, and favors web standards. Bun (JavaScriptCore, written in Zig) optimizes for speed and bundles a bundler, test runner and package manager. Shared model; the differences are engine, tooling, security defaults and maturity.",
  },
  {
    id: "comp-go-rust",
    chapter: "competitors",
    topic: "Foundations",
    level: "senior",
    q: "When would you choose Go or Rust over Node?",
    a: "When the workload is CPU-bound or needs real parallelism. Go offers goroutines over real threads plus a static binary — great for compute-plus-I/O cloud services. Rust offers peak performance with no GC and compile-time safety for hot or safety-critical paths. Node would have to offload that work to worker_threads and still wouldn't match them.",
  },
  {
    id: "arch-v8-libuv",
    chapter: "architecture",
    topic: "Foundations",
    level: "senior",
    q: "What does V8 do versus libuv, and where is the event loop?",
    a: "V8 compiles and runs JavaScript and manages its heap/GC; it knows nothing about I/O. libuv owns the event loop (the six phases), the cross-platform async I/O abstraction over the OS notifier, and the thread pool. So the event loop lives in libuv, not V8 — Node is the glue between them.",
  },
  {
    id: "arch-trace",
    chapter: "architecture",
    topic: "Foundations",
    level: "staff",
    q: "Trace what happens when you call fs.readFile.",
    a: "fs.readFile (JS) validates args and calls its C++ binding, which submits the read to libuv. With no non-blocking OS primitive for file reads, libuv runs read() on a thread-pool thread while your JS thread keeps going. On completion, libuv queues your callback in the poll phase; the next tick dequeues it and cb(err, data) runs back in JS. The thread was never blocked.",
  },
  {
    id: "errors-trycatch-async",
    chapter: "errors",
    topic: "Errors",
    level: "senior",
    q: "Why doesn't try/catch catch an error thrown inside setTimeout?",
    a: "try/catch only guards the synchronous call stack it wraps. setTimeout returns immediately, the try block completes, and the callback runs later on a fresh empty stack — the catch is gone, so the throw becomes an uncaughtException. Put the try/catch inside the callback, or wrap the work in a promise and await it so await re-threads the rejection onto a stack the catch can see.",
  },
  {
    id: "errors-unhandled-rejection",
    chapter: "errors",
    topic: "Errors",
    level: "staff",
    q: "What happens to an unhandled promise rejection in modern Node?",
    a: "Since Node 15 the default is to terminate the process — the unhandledRejection event fires and, unhandled, Node exits non-zero. The usual cause is a floating promise you neither await nor .catch(). Fix it by awaiting inside try/catch or attaching .catch(); process.on('unhandledRejection') should only log and exit, as a crash backstop rather than a handler.",
  },
  {
    id: "errors-asynclocalstorage",
    chapter: "errors",
    topic: "Errors",
    level: "staff",
    q: "How do you carry a request id through async code without threading arguments?",
    a: "AsyncLocalStorage from node:async_hooks. Call als.run(store, fn) at the start of a request; any code in that async chain — across awaits and callbacks — reads it via als.getStore(), isolated per request. It's the stable replacement for the deprecated domain module and underpins request-scoped logging, tracing and multi-tenancy.",
  },
  {
    id: "http-keepalive-agent",
    chapter: "http",
    topic: "HTTP",
    level: "senior",
    q: "What is HTTP keep-alive and why does it matter?",
    a: "Keep-alive reuses one TCP connection for many requests instead of opening a new one each time, avoiding a TCP handshake (and, on HTTPS, a TLS handshake) per request. Node manages it client-side with an Agent that pools sockets per origin; since Node 19 the global Agent defaults to keepAlive:true, so http.request and fetch reuse connections out of the box.",
  },
  {
    id: "http-1-2-3",
    chapter: "http",
    topic: "HTTP",
    level: "staff",
    q: "HTTP/1.1 vs HTTP/2 vs HTTP/3 — what changed and what does Node support?",
    a: "HTTP/1.1 carries one request at a time per connection (head-of-line blocking). HTTP/2 (node:http2 via nghttp2) multiplexes many streams over one connection with header compression, removing app-layer HOL — but still over TCP, so packet loss causes transport-level HOL. HTTP/3 runs over QUIC/UDP with independent streams to fix that; in Node core QUIC is still experimental, so HTTP/3 is usually terminated at the edge with an HTTP/2 origin.",
  },
  {
    id: "http-llhttp-security",
    chapter: "http",
    topic: "HTTP",
    level: "staff",
    q: "What is llhttp's role, and why is strict parsing a security feature?",
    a: "llhttp is the bundled C parser that turns raw socket bytes into HTTP messages incrementally. Strictness matters because lax parsing enables request smuggling and header injection: ambiguous framing (conflicting Content-Length/Transfer-Encoding), oversized header blocks, or malformed lines can desync a proxy and origin. Rejecting malformed input early — plus headersTimeout against Slowloris — makes the parser a first line of defense.",
  },
];

export const INTERVIEW_TOPICS: string[] = Array.from(new Set(INTERVIEW.map((i) => i.topic)));
